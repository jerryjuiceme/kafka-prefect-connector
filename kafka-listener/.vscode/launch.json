{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Uvicorn/kafka-listener",
      "type": "debugpy",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "module": "uvicorn",
      "justMyCode": true,
      "env": {
        "PYTHONPATH": "${cwd}"
      },
      "args": [
        "src.main:app",
        // "--reload",
        "--host",
        "0.0.0.0",
        "--port",
        "8000"
      ]
    }
  ]
}
